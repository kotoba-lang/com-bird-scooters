{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([bird-scooters.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'bird-scooters.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
